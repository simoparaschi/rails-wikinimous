<div class="nav">
  <p>Wikinimous</p>
  <div class="nav-item"><%= link_to "Create new article", new_article_path() %></div>
</div>


<div class="container">
  <% @articles.each do |article| %>
    <h1><%= link_to article.title, article_path(article) %></h1>
    <p><%= article.content %></p>
  <% end %>
</div>
